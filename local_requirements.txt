# ============================================================================
# PyTorch Lightning MNIST Project - Local Requirements
# ============================================================================
# IMPORTANT: Install PyTorch with CUDA support FIRST before installing other packages
# 
# Step 1: Install PyTorch with CUDA (choose based on your CUDA version)
#   Visit https://pytorch.org/ and select your CUDA version, then run:
#   
#   Example for CUDA 11.8:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
#   
#   Example for CUDA 12.1:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
#   
#   Example for CPU only:
#   pip install torch torchvision torchaudio
#
# Step 2: Then install the rest from this file:
#   pip install -r local_requirements.txt
# ============================================================================

# Core PyTorch packages (install separately with CUDA support - see instructions above)
# These are listed here for reference, but install from PyTorch website first
# torch>=2.0.0
# torchvision>=0.15.0
# torchaudio>=2.0.0

# PyTorch Lightning 2.0+ (REQUIRED - project uses 2.0+ API)
pytorch-lightning>=2.0.0,<3.0.0
torchmetrics>=0.11.0

# TensorBoard for visualization
tensorboard>=2.10.0
tensorboard-data-server>=0.6.0

# Data processing and visualization
numpy>=1.23.0,<2.0.0
matplotlib>=3.6.0
Pillow>=9.2.0

# Core dependencies
packaging>=21.0
PyYAML>=6.0
tqdm>=4.64.0

# HTTP and networking (for MNIST data download)
requests>=2.28.0
urllib3>=1.26.0
certifi>=2022.0.0

# File system utilities
fsspec>=2022.8.0

# Optional scientific computing packages (if needed)
scipy>=1.9.0
scikit-learn>=1.1.0
